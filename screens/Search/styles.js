import {
  StyleSheet
} from 'react-native';
import { COLORS, SIZES } from '../../constants';


const styles = StyleSheet.create({
  emptyImage: {
    width: 142,
    height: 167,
    resizeMode: 'contain',
    margin: 24
  }
})

export default styles;